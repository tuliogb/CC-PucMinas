#include <stdio.h>
#include <stdlib.h>                  
#include <string.h> 

strtok(entrada, " ");   >>>   strtok(NULL, " ");
strlen(entrada);
strcpy(entrada);
atoi(entrada);
strcmp(entrada1,entrada2)                                           "iguais == 0 / 1Maior > 0 / 1Menor < 0 "
while ((caractere = fgetc(arquivo)) != EOF);
fgets(entrada,sizeof(entrada),stdin)                                "entrada[strlen(entrada-1)] = '/0';
tolower(caractere);  toupper(caractere);
isdigit(caractere)                                                  "#include <ctype.h>"
strncat(destino,origem,10);                                         "destino + origem[ate posicao 10]";





String[] parte = linha.split(",");
int x = Integer.parseInt(numId);
String numId = linha.substring(0, linha.indexOf(","));               "(comeco,fim) >>> particao"
toUpperCase();  toLowerCase();  texto.length()
char caractere = texto.charAt(2);   e   texto.indexOf("caractere");